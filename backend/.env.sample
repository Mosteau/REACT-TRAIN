# Server Configuration
APP_PORT=3001

# CORS Configuration
FRONTEND_URL=http://localhost:3000

# Database Configuration (SQLite)
# DB_PATH=./database/fenetre_db.sqlite  # Optional: override default database path

# Authentication (if JWT is implemented later)
# JWT_SECRET=your_jwt_secret_key_here